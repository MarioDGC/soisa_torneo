name: Actualiza web con pull

on:

 push:

  branches:

   - main




jobs:

 update-local-repo:

  runs-on: ubuntu-latest




  steps:

   - name: hacer pull a local
     uses: ./ 
     with:
          ssh_key: ${{secrets.SSH_KEY_HOSTGATOR}}

     run: | 

       ssh -i private.key -p2222 marioswe@mariosweb.com './actualizar_web.sh'
